services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: honeypot-backend
    ports:
      - "8000:8000"
    environment:
      - ELASTICSEARCH_URL=${ELASTICSEARCH_URL:-http://host.docker.internal:9200}
      - JWT_SECRET=${JWT_SECRET}
      - ADMIN_USERNAME=${ADMIN_USERNAME:-admin}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD}
      - CORS_ORIGINS=${CORS_ORIGINS:-http://localhost:3000,http://127.0.0.1:3000,http://193.246.121.231:3000,http://193.246.121.232:3000,http://193.246.121.233:3000}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - honeypot-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - VITE_API_URL=${VITE_API_URL:-http://localhost:8000}
    container_name: honeypot-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - honeypot-network
    restart: unless-stopped

networks:
  honeypot-network:
    driver: bridge
